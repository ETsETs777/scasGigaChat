# Инструкция по развертыванию JustStory на локальном ПК

## 📋 Требования

Перед началом установки убедитесь, что у вас установлены следующие программы:

### 1. Node.js и npm
- **Node.js версии 18 или выше**
- Скачать можно с официального сайта: https://nodejs.org/
- При установке npm устанавливается автоматически
- Проверить установку можно командами:
  ```bash
  node --version
  npm --version
  ```

### 2. Git
- **Git для Windows**
- Скачать можно с: https://git-scm.com/download/win
- Проверить установку:
  ```bash
  git --version
  ```

### 3. Docker Desktop (для Redis)
- **Docker Desktop для Windows**
- Скачать можно с: https://www.docker.com/products/docker-desktop
- Необходим для запуска Redis
- После установки запустите Docker Desktop

## 🚀 Пошаговая установка

### Шаг 1: Клонирование репозитория

Откройте PowerShell или Command Prompt и выполните:

```bash
cd C:\Users\wsr\Desktop
git clone https://github.com/ETsETs777/scasGigaChat.git
cd scasGigaChat
```

### Шаг 2: Настройка Backend

1. **Перейдите в папку backend:**
   ```bash
   cd juststory_backend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env` в папке `juststory_backend`:**
   
   Создайте файл `.env` со следующим содержимым:
   ```env
   AI_TOKEN=ваш_токен_gigachat
   JWT_SECRET=ваш_секретный_ключ_для_jwt
   DATABASE_URL="file:./dev.db"
   ```
   
   **Важно:** 
   - Замените `ваш_токен_gigachat` на реальный токен от GigaChat API
   - Замените `ваш_секретный_ключ_для_jwt` на любой случайный секретный ключ (например, сгенерируйте через: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)

4. **Инициализируйте базу данных:**
   ```bash
   npx prisma generate
   npx prisma migrate dev
   ```

5. **Запустите Redis через Docker:**
   ```bash
   docker run --name redis -p 6379:6379 -d redis
   ```
   
   Если контейнер уже существует, используйте:
   ```bash
   docker start redis
   ```

### Шаг 3: Настройка Frontend

1. **Откройте новый терминал и перейдите в папку frontend:**
   ```bash
   cd C:\Users\wsr\Desktop\scasGigaChat\juststory_frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env.local` в папке `juststory_frontend`:**
   
   Создайте файл `.env.local` со следующим содержимым:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:4202/api
   ```

### Шаг 4: Запуск серверов

#### Запуск Backend:

В первом терминале (в папке `juststory_backend`):
```bash
npm run start:dev
```

Backend будет доступен по адресу: `http://localhost:4202`
Swagger документация: `http://localhost:4202/swagger`

#### Запуск Frontend:

Во втором терминале (в папке `juststory_frontend`):
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3000`

## ✅ Проверка работоспособности

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Вы должны увидеть главную страницу JustStory
3. Попробуйте зарегистрироваться или войти в систему

## 🔧 Полезные команды

### Backend:
```bash
npm run start:dev    # Запуск в режиме разработки
npm run build        # Сборка для продакшена
npm run start:prod   # Запуск продакшен версии
npx prisma studio    # Открыть Prisma Studio для просмотра БД
```

### Frontend:
```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен версии
npm run lint         # Проверка кода линтером
```

### Docker (Redis):
```bash
docker start redis           # Запустить Redis
docker stop redis            # Остановить Redis
docker ps                    # Показать запущенные контейнеры
docker logs redis            # Показать логи Redis
```

## 🐛 Решение проблем

### Проблема: "Port 4202 already in use"
**Решение:** Остановите процесс, использующий порт:
```bash
netstat -ano | findstr :4202
taskkill /PID <номер_процесса> /F
```

### Проблема: "Port 3000 already in use"
**Решение:** Остановите процесс, использующий порт:
```bash
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F
```

### Проблема: "Redis connection error"
**Решение:** Убедитесь, что Docker запущен и Redis контейнер работает:
```bash
docker ps
docker start redis
```

### Проблема: "Prisma client not generated"
**Решение:** Выполните:
```bash
cd juststory_backend
npx prisma generate
```

### Проблема: "Module not found" или ошибки импорта
**Решение:** Переустановите зависимости:
```bash
# В папке backend или frontend
rm -rf node_modules
npm install
```

## 📝 Примечания

- **Токен GigaChat:** Получите токен на официальном сайте GigaChat API
- **База данных:** SQLite файл будет создан автоматически в `juststory_backend/dev.db`
- **Логи:** Логи backend отображаются в терминале, где запущен `npm run start:dev`
- **Hot Reload:** Оба сервера поддерживают автоматическую перезагрузку при изменении файлов

## 🔐 Безопасность

⚠️ **Важно:** 
- Никогда не коммитьте файлы `.env` и `.env.local` в Git
- Используйте сильные секретные ключи для JWT_SECRET
- В продакшене используйте переменные окружения сервера, а не файлы .env

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в терминалах
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порты 3000 и 4202 свободны
4. Убедитесь, что Redis запущен через Docker

---

**Готово!** Теперь ваш сервер должен быть запущен и готов к работе! 🎉

